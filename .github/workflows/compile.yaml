name: Compile Check

on:
  pull_request:
    branches: 
      - 'release/jerrieee' # Only run on PRs targeting this branch
  push:
    branches: 
      - 'release/jerrieee' # Only run on pushes to this branch
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive
          # If this code lives in a specific branch, ensure we are on it
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Install ARM Toolchain
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-arm-none-eabi

      - name: Build Project
        # Based on your ls -la, the Makefile should be in the root or 'src'
        # If the Makefile is in the root of the checked-out repo:
        run: make